/**
 * This is the `baseReducer` for the application.
 *
 * This doesn't do anything as of now. For the purposes of the boilerplate
 * I have kept this as a placeholder. Based on the use cases, the name of the
 * reducer can be changed and implementation can be done based on the state
 * of the application.
 */

const initialState = {};

export default function baseReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
